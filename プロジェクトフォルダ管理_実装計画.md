# プロジェクトフォルダ管理システム 実装計画

**作成日**: 2026-01-02  
**目的**: プロジェクトフォルダを基盤としたファイル管理システムを実装し、画像アセットへの常時アクセスを可能にする

---

## 📋 概要

現在の保存ボタンを廃止し、新規作成・開く・上書き保存の3つの操作で構成される新しいファイル管理システムを実装します。すべての操作でプロジェクトフォルダを選択させることで、`images/` フォルダ内の画像に常にアクセス可能な状態を維持します。

---

## 🎯 要件定義

### 1. 新規作成ボタン
- **トリガー**: ナビゲーションバーの「新規作成」ボタンクリック
- **フロー**:
  1. A4・9:16・スクエアから比率を選択（モーダル表示）
  2. 既に編集中の場合は警告を表示（未保存の変更が失われる旨）
  3. ユーザーが OK した場合、プロジェクトフォルダを選択させる（`showDirectoryPicker()`）
  4. 選択したフォルダに `untitled-{timestamp}.html` を作成・保存
  5. フォルダハンドルとファイルハンドルを store に保存
  6. 選択した比率の初期テンプレートを読み込んでエディタに表示

### 2. 開くボタン
- **トリガー**: ナビゲーションバーの「開く」ボタンクリック、または Ctrl+O
- **フロー**:
  1. 既に編集中の場合は警告を表示
  2. ユーザーが OK した場合、プロジェクトフォルダを選択させる（`showDirectoryPicker()`）
  3. フォルダハンドルを store に保存
  4. そのフォルダ内から開くHTMLファイルを選択させる（`showOpenFilePicker()` with `startIn`）
  5. ファイルハンドルを store に保存
  6. ファイル内容を読み込んでエディタに表示

### 3. 保存ボタン
- **変更**: 完全に廃止
- **理由**: プロジェクトフォルダ選択なしでは画像アクセス不可のため

### 4. 上書き保存ボタン
- **トリガー**: ナビゲーションバーの「上書き保存」ボタンクリック、または Ctrl+S
- **フロー**:
  1. 現在開いているファイルハンドルが存在するか確認
  2. 存在する場合: デザイン領域のHTMLを書き込んで保存
  3. 存在しない場合: エラー表示（通常発生しないはず）

---

## 🏗️ 実装タスク

### Phase 1: ストア拡張（`useEditorStore.ts`）

**目的**: プロジェクトフォルダとファイルハンドルを管理

**追加する状態**:
```typescript
// プロジェクトフォルダハンドル
projectDirectoryHandle: FileSystemDirectoryHandle | null
setProjectDirectoryHandle: (handle: FileSystemDirectoryHandle | null) => void

// 現在開いているファイルハンドル
currentFileHandle: FileSystemFileHandle | null
setCurrentFileHandle: (handle: FileSystemFileHandle | null) => void

// 現在のプロジェクトフォルダ名（表示用）
projectFolderName: string
setProjectFolderName: (name: string) => void
```

**作業内容**:
- [ ] `src/store/useEditorStore.ts` に上記の状態とセッターを追加
- [ ] 型定義を `src/types/editor.ts` に追加（必要に応じて）

---

### Phase 2: ファイルシステムサービスの拡張（`fileSystem.ts`）

**目的**: File System Access API を使ったフォルダ・ファイル操作の実装

**新規関数**:

#### 1. `selectProjectFolder()`
```typescript
/**
 * プロジェクトフォルダを選択し、フォルダハンドルを返す
 */
export const selectProjectFolder = async (): Promise<FileSystemDirectoryHandle | null>
```
- `showDirectoryPicker()` を使用
- ユーザーがキャンセルした場合は null を返す
- エラーハンドリングを実装

#### 2. `createNewDesignFile()`
```typescript
/**
 * プロジェクトフォルダに新規デザインファイルを作成
 * @param directoryHandle - プロジェクトフォルダハンドル
 * @param template - 初期テンプレートHTML
 * @returns ファイルハンドル
 */
export const createNewDesignFile = async (
  directoryHandle: FileSystemDirectoryHandle,
  template: string
): Promise<FileSystemFileHandle>
```
- `untitled-{timestamp}.html` のファイル名を生成
- `directoryHandle.getFileHandle()` でファイル作成
- 初期テンプレートを書き込み
- ファイルハンドルを返す

#### 3. `openFileFromFolder()`
```typescript
/**
 * プロジェクトフォルダから既存ファイルを開く
 * @param directoryHandle - プロジェクトフォルダハンドル
 * @returns ファイルハンドルと内容
 */
export const openFileFromFolder = async (
  directoryHandle: FileSystemDirectoryHandle
): Promise<{ fileHandle: FileSystemFileHandle; content: string }>
```
- `showOpenFilePicker()` を使用（`startIn: directoryHandle`）
- `.html` ファイルのみを許可
- ファイル内容を読み込み
- ファイルハンドルと内容を返す

#### 4. `saveToCurrentFile()`
```typescript
/**
 * 現在開いているファイルに上書き保存
 * @param fileHandle - ファイルハンドル
 * @param htmlContent - 保存するHTML内容
 */
export const saveToCurrentFile = async (
  fileHandle: FileSystemFileHandle,
  htmlContent: string
): Promise<void>
```
- 既存の `saveDesignToFile()` を参考に実装
- ファイルハンドルに直接書き込み

**作業内容**:
- [ ] `src/services/fileSystem.ts` に上記の4つの関数を実装
- [ ] エラーハンドリングを各関数に追加
- [ ] 型安全性を確保

---

### Phase 3: フックの修正（`useFileSystem.ts`）

**目的**: 新しいファイルシステムサービスを使った UI ロジックの実装

**修正する関数**:

#### 1. `handleNew()` - 既存を大幅修正
```typescript
const handleNew = async () => {
  // 1. 比率選択モーダルを表示（既存のロジック）
  // 2. 未保存の変更がある場合は警告
  if (content && !confirm('未保存の変更が失われますが、よろしいですか？')) {
    return;
  }
  
  // 3. プロジェクトフォルダを選択
  const directoryHandle = await selectProjectFolder();
  if (!directoryHandle) return;
  
  // 4. ストアに保存
  setProjectDirectoryHandle(directoryHandle);
  setProjectFolderName(directoryHandle.name);
  
  // 5. 新規ファイルを作成
  const template = GET_INITIAL_TEMPLATE(selectedSize);
  const fileHandle = await createNewDesignFile(directoryHandle, template);
  
  // 6. ファイルハンドルをストアに保存
  setCurrentFileHandle(fileHandle);
  setCurrentFileName(fileHandle.name);
  
  // 7. エディタにテンプレートを読み込み
  setContent(template);
};
```

#### 2. `handleOpen()` - 既存を大幅修正
```typescript
const handleOpen = async () => {
  // 1. 未保存の変更がある場合は警告
  if (content && !confirm('未保存の変更が失われますが、よろしいですか？')) {
    return;
  }
  
  // 2. プロジェクトフォルダを選択
  const directoryHandle = await selectProjectFolder();
  if (!directoryHandle) return;
  
  // 3. ストアに保存
  setProjectDirectoryHandle(directoryHandle);
  setProjectFolderName(directoryHandle.name);
  
  // 4. フォルダ内からファイルを選択
  const { fileHandle, content: htmlContent } = await openFileFromFolder(directoryHandle);
  
  // 5. ファイルハンドルをストアに保存
  setCurrentFileHandle(fileHandle);
  setCurrentFileName(fileHandle.name);
  
  // 6. エディタに読み込み
  const designContent = extractDesignContent(htmlContent);
  const meta = parseMetaMessage(htmlContent);
  setContent(designContent);
  if (meta) setMetaMessage(meta);
};
```

#### 3. `handleSave()` - 完全に削除
- この関数を削除
- 呼び出し箇所も削除

#### 4. `handleSaveAs()` - 完全に削除または名称変更
- 「上書き保存」として機能を統合

#### 5. `handleOverwrite()` - 新規実装または既存を修正
```typescript
const handleOverwrite = async () => {
  // 1. ファイルハンドルの存在確認
  if (!currentFileHandle) {
    alert('保存先のファイルが見つかりません。');
    return;
  }
  
  // 2. HTML を構築
  const fullHTML = constructFullHTML(content, metaMessage);
  
  // 3. 上書き保存
  await saveToCurrentFile(currentFileHandle, fullHTML);
  
  // 4. 成功通知（オプション）
  console.log('保存しました:', currentFileHandle.name);
};
```

**作業内容**:
- [ ] `src/hooks/useFileSystem.ts` の各関数を修正
- [ ] 不要な関数（`handleSave`, `handleSaveAs`）を削除
- [ ] エラーハンドリングとユーザーフィードバックを追加

---

### Phase 4: アセット管理の修正（`useAssets.ts`）

**目的**: プロジェクトフォルダから画像を自動読み込み

**修正内容**:
```typescript
useEffect(() => {
  const loadImages = async () => {
    if (!projectDirectoryHandle) return;
    
    try {
      // images/ サブフォルダを取得
      const imagesHandle = await projectDirectoryHandle.getDirectoryHandle('images', { create: true });
      
      // 画像ファイルを列挙
      const files: string[] = [];
      for await (const entry of imagesHandle.values()) {
        if (entry.kind === 'file' && /\.(png|jpg|jpeg|gif|svg|webp)$/i.test(entry.name)) {
          files.push(entry.name);
          
          // Blob URL を生成
          const file = await entry.getFile();
          const url = URL.createObjectURL(file);
          setImageUrls(prev => ({ ...prev, [entry.name]: url }));
        }
      }
      setImageFiles(files);
    } catch (error) {
      console.error('画像の読み込みに失敗:', error);
    }
  };
  
  loadImages();
}, [projectDirectoryHandle]);
```

**作業内容**:
- [ ] `src/hooks/useAssets.ts` を修正
- [ ] プロジェクトフォルダハンドルを監視
- [ ] `images/` サブフォルダから画像を自動読み込み
- [ ] Blob URL のクリーンアップを実装

---

### Phase 5: UI の修正（`Navbar.tsx`）

**目的**: 保存ボタンを削除し、プロジェクトフォルダ名を表示

**修正内容**:

#### 1. 保存ボタンの削除
- `handleSave` を呼び出しているボタンを完全に削除

#### 2. プロジェクトフォルダ名の表示（オプション）
```tsx
{projectFolderName && (
  <div className="flex items-center gap-2 px-3 py-1 bg-white/5 rounded-lg">
    <FolderOpen size={14} className="text-blue-400" />
    <span className="text-xs text-gray-300">{projectFolderName}</span>
  </div>
)}
```

#### 3. ボタンラベルの修正
- 「名前を付けて保存」→ 削除
- 「上書き保存」→ 「保存」に変更（Ctrl+S）

**作業内容**:
- [ ] `src/components/common/Navbar.tsx` を修正
- [ ] 保存ボタンを削除
- [ ] プロジェクトフォルダ名の表示を追加（オプション）
- [ ] アイコンのインポートを追加（`FolderOpen`）

---

## 🔒 技術的制約と対策

### 1. ページリロード後のフォルダアクセス
**制約**: ブラウザのセキュリティにより、ページリロード後はフォルダハンドルが失われる  
**対策**:
- IndexedDB にフォルダハンドルを保存（オプション）
- ページ読み込み時に復元を試みる
- 失敗した場合は「プロジェクトフォルダを再選択してください」と表示

### 2. ブラウザ互換性
**制約**: File System Access API は Chromium ベース（Chrome, Edge）のみサポート  
**対策**:
- Safari/Firefox では従来の `<input type="file">` にフォールバック（将来的に検討）
- 現時点では Chrome/Edge 推奨と明記

### 3. セキュリティ許可
**制約**: フォルダへの書き込みには毎回ユーザー許可が必要  
**対策**:
- ユーザーフレンドリーなメッセージを表示
- 許可が拒否された場合の適切なエラー表示

---

## 📅 実装スケジュール

| Phase | 内容 | 推定時間 |
|-------|------|----------|
| Phase 1 | ストア拡張 | 15分 |
| Phase 2 | ファイルシステムサービス | 45分 |
| Phase 3 | フック修正 | 45分 |
| Phase 4 | アセット管理修正 | 30分 |
| Phase 5 | UI 修正 | 20分 |
| **合計** | | **約2時間** |

---

## ✅ 完了チェックリスト

### コード実装
- [ ] Phase 1: ストア拡張完了
- [ ] Phase 2: ファイルシステムサービス完了
- [ ] Phase 3: フック修正完了
- [ ] Phase 4: アセット管理修正完了
- [ ] Phase 5: UI 修正完了

### ビルド確認
- [ ] TypeScript コンパイル成功（`npx tsc --noEmit`）
- [ ] Vite ビルド成功（`npm run build`）
- [ ] ルール違反チェック（`@/.agent/rules/rules.md`）

---

## 🚀 実装開始

以下の順序で実装を進めます：

1. **Phase 1**: ストア拡張（`useEditorStore.ts`）
2. **Phase 2**: ファイルシステムサービス（`fileSystem.ts`）
3. **Phase 3**: フック修正（`useFileSystem.ts`）
4. **Phase 4**: アセット管理修正（`useAssets.ts`）
5. **Phase 5**: UI 修正（`Navbar.tsx`）
6. **ビルド確認**: TypeScript コンパイル → Vite ビルド → ルールチェック

**準備完了。Phase 1 から実装を開始します。**

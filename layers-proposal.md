# レイヤーメニュー・サイドバー実装案

## 1. 目的
デザイン領域内の各要素（テキスト、画像、図形）を一覧で把握し、GUI上で直感的に重ね順（z-index / DOM順序）の変更や要素の選択を行えるようにする。

## 2. ユーザーインターフェース (UI) 仕様

### 2.1 ナビゲーションバー
- **アイコン追加**: 上部メニューバー（Navbar）に「レイヤー」アイコン（Lucideの `Layers` アイコン）を追加。
- **トグル動作**: アイコンをクリックすることで、サイドバーの表示/非表示を切り替える。

### 2.2 レイヤーサイドバー
- **配置**: レイヤーメニュー選択時、画面左側に表示（メインのデザイン領域を圧迫しないよう、オーバーレイまたはワークスペースの縮小で対応）。
- **デザイン**: 
  - ダークテーマを基調とした洗練されたデザイン。
  - 各レイヤー項目ののサムネイルを縦一列に表示。（グループはすべて同一レイヤーに配置し、グループ要素は単一のサムネイルとして表示する）
  - 縦スクロール可能。

### 2.3 各要素へのレイヤーメニューの追加
- テキストボックス、図形、画像、グループのメニューに「レイヤー」ボタンを追加。
- ボタンを押すと、その要素がピックアップされた状態で、レイヤーサイドバーが表示される。

### 2.4 ドラッグ＆ドロップによる入れ替え
- **並べ替え機能**: サイドバー内の項目をドラッグして、上下に入れ替えることが可能。
- **反映**: 上に配置された項目ほど、デザイン領域でも前面（DOMの後ろ側）に表示されるように制御。

## 3. 技術的仕様・実装方針

**注意**
***rules.mdに厳密に従って実装すること***

### 3.1 4層アーキテクチャに基づく設計
`rules.md` に則り、以下の4つの層に分離して実装する。

1. **Logic (utils)**
    - `src/utils/layerUtils.ts` を新設。
    - `parseLayers(content: string)`: HTML文字列を解析し、レイヤー一覧（ID、タイプ、ラベル、グループID、サムネイル用スタイル）を抽出する。
    - `updateLayerOrder(content: string, sourceId: string, targetId: string)`: 指定されたIDの要素をDOMツリー内で移動させ、新しいHTML文字列を生成する。
2. **External Actions (services)**
    - 特筆すべき外部通信はないが、必要に応じて「レイヤーの選択状態」を `useMoveable` と同期させる。
3. **Bridge (hooks)**
    - `src/hooks/useLayers.ts` を新設。
    - `useEditorStore` の `content` を監視・同期。
    - ドラッグ＆ドロップイベントのハンドリング。
    - 複数要素（グループ）の一括移動・選択ロジックをカプセル化。
4. **UI (components)**
    - `src/components/features/LayerSidebar.tsx`: サイドバー本体。
    - `src/components/features/layer/LayerItem.tsx`: 各レイヤー項目の表示（サムネイル、名前、選択状態）。
    - `Navbar` への「レイヤー」ボタン追加。

### 3.2 レイヤーの特定とラベリング
- **特定**: `.DesignSurface` の直下にある要素を1つのレイヤーとして扱う。
- **グループの扱い**: `data-group-id` を持つ要素が連続している場合、または同一の `data-group-id` を持つ要素群は、単一の「グループ」レイヤーとして集約して表示する。
- **ラベリングルール**:
    - **テキスト**: 冒頭の数文字を表示（例: "テキスト: こんにち..."）。
    - **画像**: `img` タグの `src` または背景画像から判別。
    - **図形**: クラス名（`Circle`, `Rectangle` 等）や要素の形状から判別。

### 3.3 重ね順（z-index）の制御
- **原理**: 本エディタは DOM の出現順序がそのまま重ね順（後ろにあるほど前面）となる設計（`flattenHTML` 準拠）であるため、レイヤーの入れ替えは DOM 要素の順序変更によって行う。
- **反映**: レイヤー順序変更後、`useEditorStore.setContent` を呼び出すことでワークスペースに即座に反映させる。サイドバー内では「上が前面」として表示するため、DOM順序とは逆順にリスト表示する。

### 3.4 サムネイルの生成方法
- ブラウザのパフォーマンスを考慮し、複雑なスクリーンショット生成ではなく、以下のいずれかを採用する：
    1. **CSS プレビュー**: 要素の CSS（背景色、境界線、テキスト等）をそのまま流用し、`transform: scale(0.1)` 程度に縮小してプレビュー領域に表示。
    2. **特定アイコン**: 要素タイプに応じた Lucide アイコンを表示。
    - *初期実装では 2 を採用し、発展的に 1 または画像サムネイルの保持を検討する。*

### 3.5 ドラッグ＆ドロップ実装
- **ライブラリ**: 滑らかなアニメーションとアクセシビリティのため、`@dnd-kit/core` および `@dnd-kit/sortable` の導入を検討する。
- **UIフィードバック**: ドラッグ中は移動先のインジケーターを表示し、ドロップした瞬間に DOM および `useEditorStore` の状態を更新する。

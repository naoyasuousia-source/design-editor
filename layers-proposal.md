# レイヤーメニュー・サイドバー実装案

## 1. 目的
デザイン領域内の各要素（テキスト、画像、図形）を一覧で把握し、GUI上で直感的に重ね順（z-index / DOM順序）の変更や要素の選択を行えるようにする。

## 2. ユーザーインターフェース (UI) 仕様

### 2.1 ナビゲーションバー
- **アイコン追加**: 上部メニューバー（Navbar）に「レイヤー」アイコン（Lucideの `Layers` アイコン）を追加。
- **トグル動作**: アイコンをクリックすることで、サイドバーの表示/非表示を切り替える。

### 2.2 レイヤーサイドバー
- **配置**: 画面右側に固定表示（メインのデザイン領域を圧迫しないよう、オーバーレイまたはワークスペースの縮小で対応）。
- **デザイン**: 
  - ダークテーマを基調とした洗練されたデザイン。
  - 各レイヤー項目は縦に並び、以下の情報を含む：
    - **サムネイル**: 要素の簡易プレビュー（画像なら縮小版、図形ならその形と色、テキストなら先頭数文字）。
    - **要素タイプ・ID**: 「Text (id_1)」「Image (id_2)」などの識別情報。
    - **可視性/ロック設定**: （オプション）表示/非表示の切り替えや、編集ロックのアイコン。

### 2.3 ドラッグ＆ドロップによる入れ替え
- **並べ替え機能**: サイドバー内の項目をドラッグして、上下に入れ替えることが可能。
- **反映**: 上に配置された項目ほど、デザイン領域でも前面（DOMの後ろ側）に表示されるように制御。

## 3. 技術的仕様・実装方針

### 3.1 4-Layer Architecture に基づく構成
- **UI (components)**: 
  - `LayerSidebar.tsx`: サイドバー全体のコンテナ。
  - `LayerItem.tsx`: 各要素のリストアイテム。
- **Bridge (hooks)**: 
  - `useLayers.ts`: 現在の DOM から要素リストを抽出し、並べ替えをストアに反映するロジック。
- **Logic (utils)**: 
  - `domToLayerList`: `DesignSurface` 内の要素を走査し、レイヤーデータの配列に変換する。
  - `reorderDOMNodes`: ドラッグ終了時に、HTML 文字列内のタグ順序を入れ替える。

### 3.2 要素の特定とグループの扱い
- **フラットな要素管理**: 現在の仕様である「フラット化」に基づき、`DesignSurface` 直下の要素を 1 レイヤーとして扱う。
- **グループの表示**: `data-group-id` を持つ要素は、サイドバー上でもグループとしてまとめられ、折りたたみ（アコーディオン）表示ができると、より直感的。

### 3.3 サムネイル生成
- **方式**: 
  - パフォーマンスを優先し、リアルタイムの完全なスクリーンショットではなく、要素の CSS プロパティ（背景色、ソース URL、テキスト内容）から React コンポーネントで簡易的な「ミニプレビュー」を描画する。

## 4. 期待される効果
- 複雑に重なり合った要素（背景の図形と前面のテキストなど）の選択が容易になる。
- 前面・背面の入れ替えがコンテキストメニュー（右クリック等）なしで、視覚的に素早く行えるようになる。
- デザイン全体の構造が可視化され、制作効率が向上する。

## 5. 次のステップ
1. サイドバーの UI コンポーネント作成。
2. ドラッグ＆ドロップライブラリ（`@dnd-kit/core` 等）の選定と導入。
3. ストアの `content` (HTML文字列) を再構成するロジックの実装。

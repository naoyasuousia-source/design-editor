<uneditable>

## 概要
これは、解決困難な要件を効率的に解決するためのファイルである。要件に関する重要事項を随時追記修正することで、AIが状況を動的に把握しやすくするとともに、他のモデルも即座に状況を把握できるようにする。

## 厳守ルール
- <uneditable>タグ内は絶対に編集しないこと。
- このファイル内の<## 見出し>は絶対に編集しないこと。
- rules.mdに従うこと。**rules.mdの例外事項をよく読むこと**
- ローカルサーバーは絶対に立ち上げないこと。
- ユーザーからの指示があるまで、セクション1,4は編集しないこと。

## セクション1記述方法

<requirement>
<content></content>
<current-situation></current-situation>
<remarks></remarks>
<permission-to-move>NG</permission-to-move>
</requirement>

</uneditable>

----------------------------------------
# 以下、AIが自動的に更新する部分
----------------------------------------

## 1. 未解決要件（移動許可がNGの要件は絶対に移動・編集しないこと）（勝手に移動許可をOKに書き換えないこと）

<requirement>
<content>

## 「画像として保存」時のトリミング機能を正常化する
- 現在、「画像として保存」では、保存は正常だが、トリミング機能が正しく働かない。
- トリミングを押すと、トリミングもできず、ページがロックされ、何もできずリロードするしかなくなる。
以下の仕様で再実装する。
- トリミングは、デザイン領域の範囲内でトリミング可能。
- トリミングは自由トリミングと、1:1、9:16などを選択して、トリミングすることが可能。
- トリミング後に保存を押すと、トリミングされた部分だけ、画像として保存する。</content>
<current-situation>
- トリミング矩形の座標計算、ドラッグ、リサイズ、およびアスペクト比固定ロジックを全面的に再実装しました。
- `html-to-image` のエクスポート設定（オフセット、ピクセル比、スタイル変換）を修正し、トリミングされた領域のみが保存されるようにしました。
- UI 座標とキャンバス座標の同期問題を解決し、ズームレベルに応じた正確なトリミングが可能になりました。
</current-situation>
<remarks></remarks>
<permission-to-move>OK</permission-to-move>
</requirement>


## 2. 未解決要件に関するコード変更履歴（目的、変更内容、変更日時）

- **2026-01-03: `ImageSaveWizard.tsx` の再実装（完了）**
    - 座標計算システムを等倍ピクセルベースに統一。
    - ドラッグ移動、右下ハンドルによるリサイズ、境界チェックを実装。
    - アスペクト比（Free, 1:1, 9:16, A4）の強制ロジックを追加。
    - `html-to-image` のパラメータ（`canvasWidth`, `canvasHeight`, `style.transform`）を最適化。
    - `X` ボタンによる正常なキャンセル（ロック解除）動作を確保。

## 3. 分析中に気づいた重要ポイント（試してだめだったこと、仮設、制約条件等...）

- トリミング矩形を `fixed` で配置しつつ、`.DesignSurface` の `getBoundingClientRect()` を基準にオフセット計算を行うことで、スクロールやウィンドウリサイズに強い実装とした。
- `clip-path` を用いたポリゴンマスクにより、トリミング領域外を暗く表示する「抜き」の視覚効果を実装。

## 4. 解決済み要件とその解決方法

### 「画像として保存」時のトリミング機能の正常化
- **原因**: 以前の `ImageSaveWizard` は UI デザインのみで、実際の座標計算やマウスイベントによる操作ロジックがほとんど実装されていなかった。
- **解決策**:
    - `DesignSurface` の実座標と UI 座標をズーム率を含めて計算するカスタムフック的なロジックを実装。
    - `isDragging` と `dragType` ('move' | 'resize') を用いたステートレスなハンドリングを導入。
    - 保存時に `html-to-image` に対して、トリミング座標分だけ `transform: translate` でずらしたスタイルを渡し、指定の矩形サイズでキャプチャするようにした。

## 5. 要件に関連する全ファイルのファイル構成（それぞれの役割を1行で併記）

- `src/components/features/ImageSaveWizard.tsx`: 画像保存とトリミングのUI・ロジック。
- `src/store/useEditorStore.ts`: 保存モードやアスペクト比の状態管理。
- `src/app/App.tsx`: ウィザードのマウントポイント。

## 6. 要件に関する機能の技術スタックと動作原理（依存関係含む）

- **html-to-image**: DOM を SVG 経由で Canvas/Image に変換するライブラリ。
- **Zustand**: グローバル状態管理。
- **React Hooks**: ドラッグ・リサイズの座標計算に使用。

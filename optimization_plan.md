# コードベース最適化計画書

## 概要
`.agent/rules/rules.md` に定義された開発・実装ルールに基づき、現状のコードベースを走査・分析し、不適合箇所を修正して品質を向上させる。

## 走査対象項目
1. **型安全の徹底**: `any` の使用、型定義の不足。
2. **アーキテクチャ・層の分離**: `components` 内での DOM 操作や複雑なロジック、`hooks` での副作用管理、`src/` 直下のファイル有無。
3. **スタイリング規則**: Tailwind CSS 以外のインラインスタイルの有無。
4. **ファイル制限**: 1ファイル 300行以内。
5. **命名規則**: ディレクトリ、ファイル、コンポーネントの命名。
6. **最適化**: 未使用のインポート、変数、デッドコードの削除。パスエイリアス（`@/`）の使用徹底。

## 作業フェーズ

### フェーズ 1: 現状分析と課題抽出
- Grep や静的解析により、`any`、インラインスタイル、相対パス、300行超えファイルを特定する。
- 未使用コードの特定。

### フェーズ 2: コンポーネントとスタイルの修正
- インラインスタイルを Tailwind CSS へ移行。
- コンポーネント内の肥大化したロジックを Hooks へ抽出。

### フェーズ 3: 構造と型安全の強化
- `any` を排除し、適切な型または `unknown` への置き換え。
- 300行を超えるファイルの分割。
- 相対パスインポートを `@/` 形式へ統一。

### フェーズ 4: クリーンアップと最終確認
- デッドコードの完全削除。
- ビルド確認。

---
作成日: 2026-01-04
ステータス: 実行中

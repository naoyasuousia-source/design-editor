# 「AIへの指示」機能を改善する。

## 要件

### UI変更
- 「AI要件」（メニューバー）→「AIへの指示」
- 「AI 連携設定・設計意図」（メニューの見出し）→「AIへの指示」
- メニュー内（以下の項目に一新する）
    - 「固定ルール」：AIが編集できないルール
    **HTMLでは、<!-- USER_REQUIREMENT_START -->タグ外に記述する。**
    - 「共同編集ルール」
    - 「デザインコンセプト」
    - 「カラーデザイン」
        - primary,Secondary,Accent→メインカラー、サブカラー、アクセントカラー
        に変更。
        - それぞれ、「選択なし」も選べるようにする。
        - カラーピッカー、スポイトは維持、RGBは廃止
    
- 「設定を反映」を選択後、上書き保存する仕様にする。

## 注意
- rules.mdに従うこと。
- すべての項目の編集が、jsonに反映されるようにする。
# AI自動デザインエディタ 実装計画書（AI継承用）

## 🎯 プロジェクトの要諦
本プロジェクトは、AI（Antigravity）が生成したデザインHTMLをローカルファイルシステム経由でリアルタイムに同期し、ユーザーがGUIで微調整を行う「AI協調型デザインツール」です。

後続のAIモデルは、以下の設計思想を厳守して実装を継続してください。
1.  **出力への不干渉**: 保存されるデザインHTMLにエディタ専用のCSS/JS（Tailwind等）を混入させない。純粋なHTMLとインラインCSSのみを出力。
2.  **4層アーキテクチャ**: `UI (components)` -> `Bridge (hooks)` -> `Logic (utils/services)` の依存関係を維持。
3.  **メタデータの保護**: `ai-link-metadata` (JSON) および `data-group-id` を通じてAIと意図を共有。

---

## 🏗️ 実装ステータスと今後のタスク

### 【完了】フェーズ 1~3: 基盤・同期・承認フロー
- [x] **ファイルシステム連携**: Rootフォルダ監視 ＆ `images/` フォルダ自動生成
- [x] **AI更新検知 (AutoSync)**: Vite HMRを活用したリアルタイム検知と承認フロー
- [x] **承認UI**: 全面ロック、比較ビュー（新旧画像比較）、TemporaryBar
- [x] **履歴管理**: Zustand による Undo/Redo（履歴スタック上限設定済）
- [x] **HTML処理ユーティリティ**: JSONメタデータの埋め込み/抽出、不要プロパティ除去
- [x] **基本GUI操作**: `Moveable` による移動・リサイズ・ダブルクリックテキスト編集
- [x] **高度なスケーリング**: リサイズ時の `fontSize` 自動計算機能 ＆ キャンバス自動拡張
- [x] **ブラウザ警告・保存**: `useBeforeUnload` 警告、名前を付けて保存/上書き保存の実装

### 【進行中】フェーズ 4: GUI編集の高度化 (最優先)
1.  **アセット・サイドバーの実装**
    - [x] `fileSystemService` への一覧取得機能の追加
    - [x] `AssetSidebar` コンポーネントの作成
    - [x] HTMLファイル一覧の表示とクリックでの切り替え
    - [x] `images/` フォルダ内の画像をスキャンし、一覧表示
    - [x] 画像のプレビュー（Blob URL）表示
2.  **要素操作 ＆ ドラッグ＆ドロップ**
    - [x] 画像要素（img/div[bg]）へのドラッグ＆ドロップによる差し替え機能
    - [x] テキスト選択時のメニュー（フォント、色、サイズ調整）
    - [x] 画像選択時のメニュー（ボーダー、角丸、削除）
    - [x] **グルーピング機能**: 複数選択時のグループ化 (`data-group-id` 付与) ＆ 解除 UI
    - [x] **アスペクト比維持**: 画像リサイズ時の比率拘束ロジックの実装
    - [x] **要素IDの常時表示**: 選択中の要素の `id` をUI（またはフローティングメニュー）に常に表示する
    - [x] **直接画像差し替え**: フローティングメニュー内に「差し替え」ボタンを追加し、images内から選択可能にする
    - [x] **要素単位の追加メニュー**: 画像の簡易トリミング（object-position調整）機能の実装
3.  **システムの堅牢化とクリーンアップ (完了)**
    - [x] **自己保存の無視**: 保存直後の `design-update` 検知を無視するロジック
    - [x] **HTML出力の純粋化**: `data-last-width` などのエディタ専用属性の保存時除去
    - [x] **レスポンシブUIの調整**: ナビバー・サイドバーのレスポンシブ化
4.  **要素の追加・削除 (完了)**
    - [x] フローティングメニュー経由の要素削除
    - [x] 基本図形（長方形、テキストボックス、ダミー画像）の新規配置機能
    - [x] 新規作成時の初期テンプレート挿入

### 【完了】フェーズ 5: AI連携 ＆ 各種ウィザード
1.  **「画像として保存」ウィザードの実装**
    - [x] サブメニュー（トリミング・保存・キャンセル）の表示制御
    - [x] 領域指定のクロップUI (1:1, 9:16, 自由)
    - [x] `html-to-image` 等を用いた画像書き出しの実装
2.  **UI/UX の詳細要件の充足 (完了)**
    - [x] **ヒント・ダイアログ**: エディタの基本操作解説の実装
    - [x] **AI用メモ・ダイアログ**: `MetaMessage` の編集・同期機能
    - [x] **新規作成ウィザード**: A4/9:16/正方形の初期化テンプレート完備
3.  **画像トリミング機能 (要素単位)**
    - [x] 画像選択時のメニュー内に「トリミング」ウィザードを統合

### 【完了】フェーズ 6: 磨き上げ ＆ ハイクオリティ保証
1.  **デザインのプレミアム化**
    - [x] タイポグラフィと配色を再定義したプレミアム・ダークモード（グラスモーフィズム導入）
    - [x] ユーザー体験を向上させるマイクロインタラクション（滑らかな遷移アニメーション）
    - [x] 各OS・ブラウザでの表示微調整とアクセシビリティ対応
2.  **最終検証とバグ修正**
    - [x] 全機能の動作確認とエッジケースの修正
    - [x] コードのクリーンアップと不要なコメントの除去

---

## 🛠️ 技術実装ガイド（AIへの指示）

### 1. HTMLの整合性維持
`src/utils/htmlProcessing.ts` が全ての保存・読込の要です。
エディタで付与した `contenteditable` や `DesignSurface` などのクラスは、保存直前に必ずこのユーティリティを通してクリーンアップしてください。AIが混乱するため、Tailwindクラスは絶対に出力HTMLに含めないでください。

### 2. Moveable と Zustand の同期
GUIによる操作（移動・リサイズ終了時）は、必ず `onDragEnd` / `onResizeEnd` で `updateContentFromDOM` を呼び出し、DOMの状態をストアの `content` に書き戻してください。これが Undo/Redo のトリガーになります。

### 3. 画像の扱い
画像は必ず `./images/xxx.png` のような相対パスで扱います。`src/services/fileSystem.ts` を拡張し、画像のアップロードや読込を安全に行ってください。

---

## 📅 実装時のルール
- **言語**: 回答、コード内のコメント、計画書、ドキュメントはすべて **日本語** とする。
- **制限**: 1ファイル 300行以内を意識し、ロジックが肥大化したら Hooks/Services に切り出す。
- **セキュリティ**: 本アプリはローカル環境完結。外部APIキーなどは使用しない。

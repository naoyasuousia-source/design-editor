# AI自動デザインエディタ 開発計画書

## 📝 プロジェクト概要
ユーザーがAI（Antigravity）と協調しながらデザインをブラッシュアップできる、リアルタイム同期型のWebデザインエディタ。
AIがHTML/CSSを書き換え、エディタがその変更を検知・描画し、GUI上での高度な微調整（移動・拡大縮小・編集）を提供します。

---

## 🏗️ 実装フェーズと進捗チェックリスト

### フェーズ 1: 基盤構築 & アーキテクチャ整備
- [ ] `rules.md` に基づく 4-Layer Architecture のディレクトリ構成作成
- [ ] Tailwind CSS の基盤設定とデザイントークンの定義
- [ ] 指定の15フォント（Google Fonts連携）の読み込み設定
- [ ] 必要なライブラリの導入 (Zustand, Lucide-React, Moveable, html-to-image 等)
- [ ] 基本レイアウト（ヘッダー、ツールバー、キャンバスエリア）の実装

### フェーズ 2: 外部連携 (Services/Logic) 層 & 状態管理
- [ ] File System Access API を用いたフォルダルート選択・ファイル管理ロジック
- [ ] ファイル監視ロジック（バックグラウンドでの変更検知）
- [ ] `data-group-id` を維持しつつ、余計なタグを除去するクリーンHTML出力ロジック
- [ ] グループ化情報の永続化（カスタム属性 `data-group-id` のハンドリング）
- [ ] 保存履歴（Undo/Redo）の実装

### フェーズ 3: デザインエリア (UI) 層
- [ ] 描画エンジンの実装（読み込んだ HTML の `innerHTML` 反映）
- [ ] 初期状態（使い方説明：編集不可）の表示ロジック
- [ ] ズーム機能 (50% - 300%) の実装
- [ ] キャンバスのスクロール・中央配置制御

### フェーズ 4: AI同期ワークフロー (Bridge) 層
- [ ] 変更検知時のスクリーンショット撮影（Base64形式で旧状態を保存）
- [ ] UI 全面ロック機能と、ロック中も操作可能な一時バーの実装
- [ ] 比較ビュー（旧Base64画像と新版HTMLの並列表示）の実装
- [ ] 承認（上書き）・破棄（元に戻す）の同期ロジック

### フェーズ 5: 高度なGUI編集機能 (Features)
- [ ] **テキスト編集**: クリックでメニュー表示（指定15フォント対応）＋ 直接編集
- [ ] **要素の移動**: ドラッグ移動（`data-group-id` を持つ要素の同時連動移動）
- [ ] **拡大縮小 (Scaling)**: 
    - [ ] ホバー要素のリサイズ（子要素のレスポンシブ連動）
    - [ ] 単体素材の非等倍リサイズ ＆ はみ出し時の親要素自動追従
    - [ ] テキストボックスのフォントサイズ自動スケーリング
- [ ] **画像編集**: メニュー（枠線・角丸・差し替え・トリミング）と `images/` フォルダ連携

### フェーズ 6: 仕上げ & プレミアムUX
- [ ] ダークモードを基調とした洗練されたUIの磨き込み
- [ ] 各種マイクロアニメーション（Hover, Transition）の追加
- [ ] ヒントダイアログの実装
- [ ] キーボードショートカットの実装
    - [ ] Ctrl+S: 保存（新規なら名前付け、既存なら上書き）
    - [ ] Ctrl+O: 開く
    - [ ] Ctrl+J: ジャンプ

---

## 🛠️ 技術スタック詳細
- **Core**: React 18, TypeScript, Vite
- **Styling**: Tailwind CSS, Google Fonts
- **State**: Zustand (Selection, Grouping, History)
- **GUI Engine**: react-moveable
- **Comparison**: html-to-image

---

## 📅 設計・運用ルール
- **フォント制約**: 指定の15種類（Noto Sans JP, Inter等）を厳守し、AIにも共有。
- **グループ永続化**: 階層構造は変えず、`data-group-id` 属性で論理グループを識別。
- **クリーンな保存**: `Moveable` 等の制御用タグを排除し、純粋なHTML+インラインCSSを出力。
- **リアルタイム同期**: AIによるファイル更新を検知し、即座に比較フローへ移行。

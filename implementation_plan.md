# AI自動デザインエディタ 開発計画書

## 📝 プロジェクト概要
ユーザーがAI（Antigravity）と協調しながら一流のデザインを構築できる、リアルタイム同期型Webエディタ。
HTML内の不可視なメタメッセージを通じてAIと意図を共有し、ウィンドウ幅に依存せず、GUIとAIの変更をシームレスに同期・管理・保存します。

---

## 🏗️ 実装フェーズと進捗チェックリスト

### フェーズ 1: 基盤構築 & デザインシステム
- [x] `rules.md` に基づく 4-Layer Architecture のディレクトリ構成作成
- [x] Tailwind CSS デザイントークン設定（超レスポンシブなラップ型ヘッダー含む）
- [x] **指定15フォントの導入**:
    - Noto Sans JP, Noto Serif JP, 游ゴシック, 游明朝, Meiryo, M PLUS 1p, Zen 角ゴシック New
    - Inter, Montserrat, Roboto, Playfair Display, Oswald, Poppins, JetBrains Mono, Times New Roman
- [x] 基本レイアウト：ウィンドウ幅に応じて見切れない「アダプティブ・ツールバー」の実装

### 【最優先・実装漏れ修正】AI連携基盤の完成
- [x] **MetaMessage の状態管理統合**: `EditorState` に要求、コンセプト、配色等の情報を保持
- [x] **HTML パーサー ＆ クリーンアップエンジン**:
    - [x] `id`, `data-group-id` 以外の不要な属性（`contentEditable`等）の自動除去
    - [x] HTML内の隠しエリア（JSONデータ）のパース ＆ 書き出し同期
- [x] **テキスト編集の高度化**: テキストボックス内へ子要素混入制限 ＆ リサイズ時の自動フォントサイズ調整
- [x] **保存ロジックの統合**: 「保存(Save As)」と「上書き保存」を整理。ハンドルがない場合は名前付けへ。
- [x] **ブラウザ終了警告 (beforeunload)**: `isDirty` フラグに基づくリロード・終了時の確認ダイアログ
- [x] **キャンバス自動拡張**: 要素がキャンバス外にはみ出した際、画像比率を保ちつつ親要素を自動リサイズするロジック
- [x] **ズーム機能の正規化**: 50%～300%の範囲での無段階または多段階ズーム調整

### フェーズ 2: 高度な状態管理 & 保存ロジック (Services/Logic)
- [x] **履歴管理スタック (Snapshot Engine)**: Undo/Redo の実装。GUI操作とAI同期の両方を正確に管理
- [x] **Dirtyチェック**: 編集状態のフラグ管理（新規作成時の警告用）
- [ ] **メタメッセージ自動更新エンジン**: 保存時に現在の修正要求、コンセプト、配色情報を自動集計・日本語でHTMLに埋め込む
- [ ] **HTMLパージロジック**: `Moveable` の補助線やエディタ専用クラスを完全に除去し、`id` / `data-group-id` のみを残したクリーンHTMLを出力
- [x] File System Access API による「フォルダ単位」のアクセス・監視ロジック

### フェーズ 3: 描画 & 同期ワークフロー (UI/Bridge)
- [x] デザインエリアの描画エンジン（`innerHTML` 反映）
- [x] 初期状態（使い方説明：編集不可）の表示ロジック
- [x] 更新検知時の **キャンバス・スクリーンショット（Base64形式）退避**
- [x] UI 全面ロック ＆ 一時バー（承認・破棄・比較）の連動ロジック
- [x] 比較ビュー（旧Base64画像 vs 新版HTML）の実装

### フェーズ 4: 高度なGUI編集機能 (Features)
- [x] **移動 ＆ グループ選択**: ドラッグ移動（`data-group-id` による一括連動。境界線制限あり）
- [ ] **画像素材の操作**:
    - [ ] `images/` フォルダ内の画像一覧取得 ＆ ドラッグ＆ドロップによる差し替え機能
- [x] **スケーリング (Moveable連携)**:
    - [x] ホバー要素のリサイズ（子要素のレスポンシブ連動）
    - [x] テキストボックスの自動フォントサイズ・スケーリング
- [ ] **要素の追加・削除機能**: GUI 上からの直接要素削除 ＆ 基本図形（div/p）の追加
- [x] **テキスト編集**: ダブルクリックでの直接編集（キャレット挿入）

### フェーズ 5: AI連携 ＆ 各種ウィザード
- [ ] **新規作成ウィザード**: A4/9:16/スクエア選択。Dirtyフラグに基づき適切に警告を表示
- [ ] **AI用メモ・ダイアログ**: HTML内のメタメッセージをUI上で上書き編集・自動反映
- [ ] **画像書き出し ＆ トリミング**:
    - [ ] キャンバス比率での直接保存機能
    - [ ] トリミングモードの実装（自由トリミング ＋ 1:1 / 9:16 プリセット選択）
    - [ ] `images/` フォルダ連携（画像差し替え、トリミング後保存）

### フェーズ 6: 仕上げ & プレミアムUX
- [ ] ブラック基調の洗練されたダークモードのデザイン完成
- [ ] OS/ブラウザを選ばない「絶対に見切れないUI」の徹底検証
- [ ] 各種マイクロアニメーション（Hover, Transition）の追加
- [x] **ショートカットキー最適化**: 
    - Ctrl+S: 保存
    - Ctrl+O: 開く

---

## 🛠️ 技術スタック詳細
- **Core**: React 18, TypeScript, Vite
- **Styling**: Tailwind CSS, Google Fonts
- **State**: Zustand (Selection, History Stack, Dirty State)
- **GUI Engine**: react-moveable
- **Comparison**: html-to-image

---

## 📅 設計・運用ルール（最重要）
- **履歴の整合性**: GUI操作で発生した変更と、AIの変更を一つの履歴スタックで管理。
- **メタメッセージの全更新**: 保存ごとにAI用メモエリア（要求・コンセプト等）を必ず最新化。
- **構造の純粋性**: テキストボックスは常に終端要素（Leaf Node）として維持。
- **アダプティブUI**: すべてのUI要素はウィンドウ幅に応じてラップし、画面端で見切れない配置を動的に行う。

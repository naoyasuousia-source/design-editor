# AI自動デザインエディタ 開発計画書

## 📝 プロジェクト概要
ユーザーがAI（Antigravity）と協調しながらデザインをブラッシュアップできる、リアルタイム同期型のWebデザインエディタ。
AIがHTML/CSSを書き換え、エディタがその変更を検知・描画し、GUI上での高度な微調整（移動・拡大縮小・編集）を提供します。

---

## 🏗️ 実装フェーズと進捗チェックリスト

### フェーズ 1: 基盤構築 & アーキテクチャ整備
- [ ] `rules.md` に基づく 4-Layer Architecture のディレクトリ構成作成
- [ ] Tailwind CSS の基盤設定とデザイントークンの定義
- [ ] 必要なライブラリの導入 (Zustand, Lucide-React, Moveable, html-to-image 等)
- [ ] 基本レイアウト（ヘッダー、ツールバー、キャンバスエリア）の実装

### フェーズ 2: 外部連携 (Services/Logic) 層 & 状態管理
- [ ] File System Access API を用いたフォルダルート選択ロジックの実装
- [ ] ファイル監視ロジック（バックグラウンドでの変更検知）の実装
- [ ] クリーンなHTML出力ロジックの実装（エディタ用の一時タグを除去）
- [ ] グループ化情報の状態管理（HTML階層を壊さないエディタ内管理）
- [ ] 保存履歴（Undo/Redo）のステート管理実装

### フェーズ 3: デザインエリア (UI) 層
- [ ] 描画エンジンの実装（読み込んだ HTML の `innerHTML` 反映）
- [ ] 初期状態（使い方説明：編集不可）の表示ロジック
- [ ] ズーム機能 (50% - 300%) の実装
- [ ] キャンバスのスクロール・中央配置制御

### フェーズ 4: AI同期ワークフロー (Bridge) 層
- [ ] 外部変更検知時の UI 全面ロック機能
- [ ] 一時バー（承認・破棄・比較）の実装。UIロック中も操作可能にする。
- [ ] 比較ビュー（変更前後のスクリーンショット並列表示）の実装

### フェーズ 5: 高度なGUI編集機能 (Features)
- [ ] **テキスト編集**: クリックでメニュー表示（色・フォント等）＋ キャレット挿入による直接編集
- [ ] **要素の移動**: ドラッグによる移動（グループ・子要素も連動。境界線制限あり）
- [ ] **拡大縮小 (Scaling)**: 
    - [ ] ホバー要素のリサイズ機能（子要素もレスポンシブに連動）
    - [ ] 単体テキスト/図形の非等倍（縦横のみ）拡大縮小
    - [ ] 親要素の自動追従（子要素がはみ出す際のリサイズ）
    - [ ] テキストボックスの自動フォントサイズ調整（内容の可視性維持）
- [ ] **グループ化**: エディタ内での論理的グループ管理（移動・リサイズの共通化）
- [ ] **画像編集**: メニュー（枠線・角丸・差し替え・トリミング）と `images/` フォルダ連携

### フェーズ 6: 仕上げ & プレミアムUX
- [ ] ダークモードを基調とした洗練されたUIの磨き込み
- [ ] 各種マイクロアニメーション（Hover, Transition）の追加
- [ ] ヒントダイアログの実装
- [ ] キーボードショートカット (Ctrl+S, O, N, J:ジャンプ等) の実装

---

## 🛠️ 技術スタック詳細
- **Core**: React 18, TypeScript, Vite
- **Styling**: Tailwind CSS
- **State**: Zustand (Selection, Grouping, Undo/Redo)
- **GUI Engine**: react-moveable (移動・拡大縮小の制御)
- **File API**: Web File System Access API
- **Comparison**: html-to-image (スクショ比較用)

---

## 📅 設計ルール再確認
- すべてのファイルは `src/` 配下の指定ディレクトリに配置。
- **HTML出力**: 余計なタグを除去し、インラインCSSを用いたクリーンなHTMLを出力。
- **テキストボックス**: 子要素を配置できない終端ノードとして扱う。
- **グループ化**: HTMLの階層構造を変更せず、構成データとしてエディタ側で保持。

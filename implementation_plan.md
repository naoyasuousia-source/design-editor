# AI自動デザインエディタ 開発計画書

## 📝 プロジェクト概要
ユーザーがAI（Antigravity）と協調しながら一流のデザインを構築できる、リアルタイム同期型Webエディタ。
HTML内の不可視なメタメッセージを通じてAIと意図を共有し、ウィンドウ幅に依存せず、GUIとAIの変更をシームレスに同期・管理・保存します。

---

## 🏗️ 実装フェーズと進捗チェックリスト

### フェーズ 1: 基盤構築 & デザインシステム
- [x] `rules.md` に基づく 4-Layer Architecture のディレクトリ構成作成
- [x] Tailwind CSS デザイントークン設定（超レスポンシブなラップ型ヘッダー含む）
- [ ] **指定15フォントの導入**:
    - Noto Sans JP, Noto Serif JP, 游ゴシック, 游明朝, Meiryo, M PLUS 1p, Zen 角ゴシック New
    - Inter, Montserrat, Roboto, Playfair Display, Oswald, Poppins, JetBrains Mono, Times New Roman
- [x] 基本レイアウト：ウィンドウ幅に応じて見切れない「アダプティブ・ツールバー」の実装

### フェーズ 2: 高度な状態管理 & 保存ロジック (Services/Logic)
- [ ] **履歴管理スタック (Snapshot Engine)**: Undo/Redo の実装。GUI操作とAI同期の両方を正確に管理
- [ ] **Dirtyチェック**: 編集状態のフラグ管理（新規作成時の警告用）
- [ ] **メタメッセージ自動更新エンジン**: 保存時に現在の修正要求、コンセプト、配色情報を自動集計・日本語でHTMLに埋め込む
- [ ] **HTMLパージロジック**: `Moveable` の補助線やエディタ専用クラスを完全に除去し、`id` / `data-group-id` のみを残したクリーンHTMLを出力
- [ ] File System Access API による「フォルダ単位」のアクセス・監視ロジック

### フェーズ 3: 描画 & 同期ワークフロー (UI/Bridge)
- [ ] デザインエリアの描画エンジン（`innerHTML` 反映）
- [ ] 初期状態（使い方説明：編集不可）の表示ロジック
- [ ] 更新検知時の **キャンバス・スクリーンショット（Base64形式）退避**
- [ ] UI 全面ロック ＆ 一時バー（承認・破棄・比較）の連動ロジック
- [ ] 比較ビュー（旧Base64画像 vs 新版HTML）の実装

### フェーズ 4: 高度なGUI編集機能 (Features)
- [ ] **移動 ＆ グループ選択**: ドラッグ移動（`data-group-id` による一括連動。境界線制限あり）
- [ ] **スケーリング (Moveable連携)**:
    - [ ] ホバー要素のリサイズ（子要素のレスポンシブ連動）
    - [ ] 単体素材の非等倍リサイズ ＆ はみ出し時の親要素自動拡張
    - [ ] テキストボックスの自動フォントサイズ・スケーリング
- [ ] **構造バリデーション**: テキストボックス内へ子要素が混入しないよう GUI 操作時に制限
- [ ] **テキスト編集**: メニュー表示 ＋ 直接編集（キャレット挿入）

### フェーズ 5: AI連携 ＆ 各種ウィザード
- [ ] **新規作成ウィザード**: A4/9:16/スクエア選択。Dirtyフラグに基づき適切に警告を表示
- [ ] **AI用メモ・ダイアログ**: HTML内のメタメッセージをUI上で上書き編集・自動反映
- [ ] **画像編集 ＆ 抽出**: `images/` フォルダ連携。トリミング機能付き画像書き出し

### フェーズ 6: 仕上げ & プレミアムUX
- [ ] ブラック基調の洗練されたダークモードのデザイン完成
- [ ] OS/ブラウザを選ばない「絶対に見切れないUI」の徹底検証
- [ ] 各種マイクロアニメーション（Hover, Transition）の追加
- [ ] **ショートカットキー最適化**: 
    - Ctrl+S: 保存（新規なら名前付け、既存なら上書き）
    - Ctrl+O: 開く
    - ※Ctrl+J、Ctrl+N はブラウザ標準との競合回避のため除外

---

## 🛠️ 技術スタック詳細
- **Core**: React 18, TypeScript, Vite
- **Styling**: Tailwind CSS, Google Fonts
- **State**: Zustand (Selection, History Stack, Dirty State)
- **GUI Engine**: react-moveable
- **Comparison**: html-to-image

---

## 📅 設計・運用ルール（最重要）
- **履歴の整合性**: GUI操作で発生した変更と、AIの変更を一つの履歴スタックで管理。
- **メタメッセージの全更新**: 保存ごとにAI用メモエリア（要求・コンセプト等）を必ず最新化。
- **構造の純粋性**: テキストボックスは常に終端要素（Leaf Node）として維持。
- **アダプティブUI**: すべてのUI要素はウィンドウ幅に応じてラップし、画面端で見切れない配置を動的に行う。

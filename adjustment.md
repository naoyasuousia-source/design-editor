<uneditable>

## 概要
これは、解決困難な要件を効率的に解決するためのファイルである。要件に関する重要事項を随時追記修正することで、AIが状況を動的に把握しやすくするとともに、他のモデルも即座に状況を把握できるようにする。

## 厳守ルール
- <uneditable>タグ内は絶対に編集しないこと。
- このファイル内の<## 見出し>は絶対に編集しないこと。
- rules.mdに従うこと。**rules.mdの例外事項をよく読むこと**
- ローカルサーバーは絶対に立ち上げないこと。
- ユーザーからの指示があるまで、セクション1,4は編集しないこと。

## セクション1記述方法

<requirement>
<content></content>
<current-situation></current-situation>
<remarks></remarks>
<permission-to-move>NG</permission-to-move>
</requirement>

</uneditable>

----------------------------------------
# 以下、AIが自動的に更新する部分
----------------------------------------

## 1. 未解決要件（移動許可がNGの要件は絶対に移動・編集しないこと）（勝手に移動許可をOKに書き換えないこと）

<requirement>
<content>親要素内のテキストボックスをクリック場合、親要素を選択するのではなく、テキストボックスそのものを直接選択するようにする。</content>
<current-situation>現在は、親要素内のテキストボックスをクリックすると、親要素が選択され、親要素の拡大縮小ポイントが表示される。</current-situation>
<remarks></remarks>
<permission-to-move>NG</permission-to-move>
</requirement>


## 2. 未解決要件に関するコード変更履歴（目的、変更内容、変更日時）

### 2026-01-02 09:50 - 階層構造内の子要素を直接選択可能に修正

**目的**: 親要素（コンテナ）の中にあるテキストボックスなどをクリックした際、親ではなくクリックした要素そのものを選択できるようにする。

**変更内容**:
1. `src/hooks/useMoveable.ts`
   - `handleCanvasClick`: これまでは `DesignSurface` の直接の子要素（最上位の親）を再帰的に探して選択していたが、クリックされたターゲットが `DesignSurface` 内であればその要素自体を `targets` として設定するように変更。

**結果**: 
- 階層化されたデザイン（テンプレートのコンテナ内テキストなど）において、個別の要素をピンポイントで選択・編集可能になった。

## 3. 分析中に気づいた重要ポイント（試してだめだったこと、仮設、制約条件等...）

### 要素選択の仕様変更
- 以前のコードは「常に最上位の div を掴む」という挙動を意図していたが、これではネストされたテキストの編集が困難（一度上位を選択してから再度クリックが必要など）になるため、今回の変更で直感的な「クリックしたものを掴む」挙動に統一した。
- `closest('.DesignSurface')` を使用することで、デザイン領域外の UI 要素を誤って選択しないようガードしている。

## 4. 解決済み要件とその解決方法

### 親要素内の要素を直接選択できない問題
**解決方法**: `useMoveable.ts` の `handleCanvasClick` における親要素への遡りループを削除し、ターゲット要素を直接 `Moveable` の対象にするよう修正。


## 5. 要件に関連する全ファイルのファイル構成（それぞれの役割を1行で併記）

```
src/hooks/useMoveable.ts       - GUI編集フック（要素選択、ドラッグ操作、DOM→content同期、階層選択ロジック）
src/components/features/Workspace.tsx - デザイン領域表示コンポーネント（content描画、イベントハンドリング集約）
src/store/useEditorStore.ts    - グローバル状態管理（content, pageSize 等）
```

## 6. 要件に関する機能の技術スタックと動作原理（依存関係含む）

### 要素選択ロジック
- **ターゲット特定**: `onMouseDown` イベント時に `e.target` を取得。
- **範囲制限**: `el.closest('.DesignSurface')` を使用し、デザイン領域内の要素であることを担保。
- **階層選択**: 親要素への遡行を廃止し、クリックされた末端の要素（Leaf element）を選択可能に。
- **Moveable連携**: `setTargets([el])` を通じて `react-moveable` の編集枠を動的に適用。


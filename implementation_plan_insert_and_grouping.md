# 実装計画書：フラット化・グループ化・挿入機能の刷新

## 1. 概要
AIが生成する高度な構造（入れ子）を維持しつつ、ユーザーがエディタ上でCanvaのように直感的に（フラットに）操作できる「自動フラット化ハイブリッドシステム」を実装します。また、サイドバーを廃止し、利便性の高い「挿入」メニューをツールバーに追加します。

## 2. 実装タスク

### フェーズ1：既存UIの整理と挿入機能の基盤
- [ ] **AssetSidebarの廃止**: `src/app/App.tsx` からの削除およびコンポーネントの整理。
- [ ] **Navbarへの「挿入」ボタン追加**: 
    - 「テキスト」「画像」のドロップダウンメニュー実装。
    - アイコン：`Lucide.Plus` を使用。
- [ ] **手動挿入ロジックの実装**: 
    - `DesignSurface` の末尾にフラットな状態で要素を追加。
    - 初期配置（キャンバス中央付近）の計算。

### フェーズ2：自動フラット化エンジンの開発
- [ ] **`src/utils/htmlProcessing.ts` の拡張**: 
    - `flattenHTML(html: string)` 関数の実装。
    - DOMParser を使い、再帰的に offsets を計算、全要素を絶対座標（top, left）に変換してフラットな配列として再構成する。
    - 親要素がスタイル（色・枠線）を持つ場合、それを「最背面の形状要素」として独立させる。
- [ ] **ストア連携**: 
    - `detectExternalUpdate` 時に `flattenHTML` を呼び出すように変更。

### フェーズ3：グループ化と高度な選択UI（オレンジ枠システム）
- [ ] **グループ選択ロジック (`useSelection.ts`)**:
    - 1クリック目：`data-group-id` を持つ要素をクリックした場合、そのグループ全体を選択状態にする（オレンジ枠モード）。
    - 2クリック目：同一要素を再度クリック、またはグループ内の他要素をクリックした場合、個別選択（水色枠モード）を併記。
- [ ] **MoveableManagerの拡張**:
    - グループ選択時（オレンジ枠）：
        - `target` をグループ全要素に設定。
        - スタイルをオレンジ（2倍の太さ）、四隅のハンドルのみに限定。
        - 縦横比固定のレスポンシブ拡大縮小を強制。
    - 個別選択時（水色枠）：
        - 通常の Moveable 操作。
- [ ] **グループ操作メニュー**:
    - フローティングメニューに「グループ化」「グループ解除」「グループ削除」を追加。

### フェーズ4：データ永続化とクリーンアップ
- [ ] **HTML保存ロジック**: `data-group-id` を維持したままクリーンなHTMLを出力する。
- [ ] **既存ロジックの削除**: `useTransform.ts` などにある古いレスポンシブ入れ子管理ロジックを整理。

## 3. 質問・確認事項
1. **オレンジ枠の挙動**: 「2回クリックで個別選択」の際、オレンジ枠（グループ全体）と水色枠（個別）が「同時に表示」されるという理解で合っていますか？（個別編集しつつグループの範囲も視覚化するイメージ）
2. **画像の挿入元**: `images/` フォルダから選択する際、現在の `AssetSidebar` にあったようなサムネイル一覧を出す簡易的な「画像ピッカー（モーダル）」を作成する方針で良いでしょうか？
3. **拡大縮小の制約**: グループ選択時（オレンジ枠）は、`resizable` で方向を一方向に限らず、常に全方向均等（自由なアス比ではなく、内容の比率を維持したリサイズ）という理解で良いでしょうか？

## 4. 期待される成果
- AIは `flex` などで自由に美しいレイアウトを書き続けられる。
- ユーザーは複雑なDOM階層を意識せず、全てのパーツを自由にバラバラまたはまとめて動かせる。
- UIがスッキリし、クリエイティブな作業に集中できる。

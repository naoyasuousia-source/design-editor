# 実装計画：ルール遵守およびホーム画面の改善

## 1. 概要
プロジェクトのルール（`.agent/rules/rules.md`）に準拠するようにコードベースを整理し、ユーザーの要望に基づきホーム画面をプレミアムなデザインに改善します。

## 2. タスク

### 2.1 コードベースの整理（ルール遵守）
- [x] **ディレクトリ構造の整理**: `src/components/features/` 内のファイルを適切なサブディレクトリに移動し、パスを修正する。
- [x] **デッドコードの削除**: 未使用のインポートや変数を調査し、削除する。
- [x] **スタイルの最適化**: インラインスタイルを可能な限り Tailwind CSS に移行する。
- [x] **型安全性の向上**: `any` 型の使用がないか再調査し、あれば修正する。

### 2.2 ホーム画面の改善
- [x] **Home コンポーネントの作成**: `src/components/features/home/Home.tsx` を新規作成する。
    - プロジェクトフォルダ選択ボタン、最近のプロジェクト表示などの機能。
    - プレミアムなダークモードデザイン、マイクロアニメーションの導入。
- [x] **App.tsx のルーティング修正**: `currentFileHandle` が存在しない場合にホーム画面を表示するように変更する。
- [x] **アセットの準備**: `generate_image` ツールを使用して、ホーム画面で使用する美しい背景画像やアイコンを生成する。
- [x] **300行ルールの適用**: `ImageSaveWizard.tsx` と `htmlProcessing.ts` を機能ごとに分割し、1ファイル300行以内を遵守。

## 3. スケジュール
1. ディレクトリ構造の整理とパス修正
2. コードのクリーンアップ（未使用コード、インラインスタイル）
3. ホーム画面の設計と実装
4. App.tsx への統合と最終確認

# 要素の取り扱いと挿入機能

## ASSETSの削除
- 現在のASSETSのUIは廃止する。

## 挿入メニュー追加
- メニューバーに「挿入ボタン」を追加する。
- 挿入ボタンをクリックすると、画像、テキストボックス選択できる。
- テキストボックスを選択すると、デフォルトのテキストボックスが挿入される。
- 画像を選択すると、images/の中から画像を選択して挿入する。
**いずれもグループに属していない要素として追加される。**

## 要素の扱いの改革
以下のように要素の扱いを大改革する。
### AI生成時
AIは、複合要素を生成する場合、親子階層構造を使って、余白等に偏りのない、美しいデザインを生成する。

### エディタ反映時
- エディタに反映される際に、親子関係は解消し、フラットな関係に変換する。
**エディタ上では親子という概念は無く、すべてルート直下のフラットな要素として扱う。**
- 元の親子関係にあったすべての要素はフラットな要素が集合したひとつのグループとして再定義してレンダリングする。

### UI
- グループ化されてるすべての要素は、一回クリックすると、水色枠と水色ポイントは表示せず、グループ全体の外枠のオレンジ枠(水色枠の2倍の太さ）と、四隅のみにオレンジポイントを表示する。
- この状態でオレンジポイントを操作すると、現在の親子要素で実装しているように、グループ内のレイアウト、サイズ比率を一切変えずに、全要素完全レスポンシブでまとめて拡大縮小可能。
**一方向への伸長収縮は不可**
- オレンジ枠とオレンジポイントのみが表示され、グループ全体が選択されている状態で、もう一度、同じ要素をクリックすると、オレンジ枠とオレンジポイントは表示されたまま。水色枠と水色ポイントも表示され、グループ内の要素が個別に選択可能になる。
- グループ全体を選択している場合、グループ解除ボタンと削除ボタン、グループidとidコピーボタンを含むグループ特別メニューが表示され、グループ解除、グループごと削除を可能にする。
- ユーザーはシフトキーを使って、複数要素を選択した場合、「グループ化」メニューが表示され、要素をグループ化することができる。

# ホーム画面改善計画

## 1. 目的
`home.md` の要件に基づき、エディタ全体の雰囲気を演出しつつ、未接続時の操作を制限した直感的なホーム体験を実装する。

## 2. タスク
- [x] **メニューバーの常時表示と制御**:
    - [x] ホーム（ファイル未選択時）でも `Navbar` を表示。
    - [x] 「新規作成」「開く」「ヒント」のみを有効化し、アニメーションでハイライト。
    - [x] それ以外のメニュー（保存、挿入、AI指示等）を無効化（opacity低下・click無効）。
- [x] **ワークスペースへの広告デザイン表示**:
    - [x] `HOME_AD_HTML` テンプレートを作成。
    - [x] `generate_image` でエディタの雰囲気に合った広告用ビジュアルを追加。
    - [x] ホーム時は `Workspace` 内にこの固定デザインを中央表示（ロック状態）。
- [x] **全体構成の調整**:
    - [x] `App.tsx` の条件分岐を修正し、ホーム画面とエディタ画面をシームレスに統合。
    - [x] ホーム時のデザインは編集不可（Workspaceロック）に設定。
- [x] **デッドコード整理**:
    - [x] 不要になった古い `Home.tsx` のルーティングや参照を確認。

## 3. 実装詳細
- `Navbar.tsx`: `currentFileHandle` の有無でボタンの `disabled` と `className` (highlight) を切り替え。
- `Workspace.tsx`: `isHome` プロップを受け取り、ホーム時は `HOME_AD_HTML` を強制。
- `adTemplate.ts`: かっこいい広告風HTMLを定義。
- `App.tsx`: 常に `Navbar` と `Workspace` を表示する単一レイアウトに統一。

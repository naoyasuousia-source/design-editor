# 要素回転機能の実装

## 要件
- テキストボックス、画像、図形、グループすべてにおいて、要素選択時、回転ハンドルをUI上で要素の近くに表示する。
- 回転ハンドルは、白丸背景の中に、二方向の矢印が配置されているデザインで、uiライブラリを利用するか、最悪svgで自作する。
- 回転ハンドルをドラッグ操作すると要素が連続的に回転する。
- また、回転ハンドルをワンクリックすると、回転メニューが展開される。
- 回転メニューは、「90°回転」「リセット」アイコンのみとする。
- 90°回転は、いかなる回転状態であっても、追加で左に90度単位で回転させる。
- リセットは、要素の回転角度を0度に戻す。


## グループの回転
- グループ内の要素同士の位置関係は完全に保ったまま、回転する。
- グループ内の要素はグループ化された時点の傾き状態を0°とし、その状態から回転する。（グループ回転時、グループ内の要素の傾きは正規化しないで回転を追加するだけにする）
- リセットが選択されたら、個別要素を0度に戻すのではなく、グループ作成時点の傾き状態に戻す。

## 重要
- transform: rotate(Ndeg) 方式に統一すること。
- **rules.mdに厳密に従うこと。**
特に以下のルールは必ず順守すること。
- **「UI → Hooks → Services/Utils → （Store）→ UI」の単方向フローで設計し、React の宣言的 UI と命令的なロジックを厳密に分離せよ。**
- **Forbidden Non-Ref DOM Manipulation**: 
    - `ref` を介さない破壊的なDOM操作は厳禁とし、React外部での全ての DOM 操作 は、必ず hooks 層から渡された `ref.current` を対象に実行せよ。
    - `ref`を介した命令的なDOM操作はドラッグ中のプレビュー等の「一時的な視覚効果」に限定し、操作終了後は直ちに最終データを hooks を通じて State へ同期せよ。DOM の最終状態は、必ず React の宣言的レンダリングによって確定・収束させなければならない。
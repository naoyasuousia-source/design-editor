# AI自動デザインエディタの作成

## 設計思想

- Webアプリとして提供する。
- ユーザーは、デザイン用HTMLを、本エディタで開きつつ、Antigravityに読み込ませる。
- Antigravityは、要求されたデザインをHTMLで作成する。（テキスト、画像、デザイン要素含有可能）
- エディタは、読み込みファイルの上書きを検知し、エディタ上にデザインを描画する。
- ユーザーは、エディタ上で、GUIでデザインの微調整を行うことができる。
- 以上の設計により、ユーザーにリアルタイム同期AIデザイン体験を提供する。

## 条件

- **rules.mdに厳密に従う。**
- **特にディレクトリ構造はrules.mdに厳密に従う。**
- フロントエンド完結で、完全無料で開発する。
- PC環境かつchromeまたはedge推奨とし、それ以外の環境への対応は考えない。

## スタック

- React(TS)
- Vite
- スタイリングは原則TailwindCSS限定
- その他、適宜必要な無料ライブラリ

## UI

- 上部メニューバー
**メニューバーはウィンドウ幅に対応して、常にすべて表示されるように折り返し表示する。**
**すべてのUI、メニュー、サブメニューは、ウィンドウ幅が狭くなっても絶対に見切れないように工夫する。**
    - アイコン（AI-Link Design）
    - 「新規作成ボタン」
        - A4・9:16・スクエアから選択可能。
        - ページ読み込み直後は、警告なく選択可能。すでにエディタ上で編集中の場合は、警告を出し、OKなら新規作成可能。
         - 比率選択後、プロジェクトフォルダ選択させる。（フォルダ内の画像へのアクセスを可能にするため）
         - プロジェクトフォルダ選択後、ファイル名を入力させたのち、そのフォルダに空のデザインHTMLを保存する。
         - 保存後、その空のファイルを開き、エディタ画面に表示する。
        - **サイト全体のHTMLを保存するのではなく、デザイン領域のHTMLのみを保存する。**

    - 「開くボタン」
        - ローカルのデザインHTMLを開く。
        - ctrl+Oでも可能にする。
        - **後述のimagesフォルダにもアクセスするため、プロジェクトフォルダごと選択させ、その後そのフォルダから開くデザインHTMLを選ばせる。**
    
    - 「上書き保存ボタン」
        - ローカルのデザインHTMLを上書き保存する。
        - ダイアログ出す。
        - ctrl+Sでも可能にする。
    - 「画像として保存ボタン」
        - デザイン領域がロックされるとともに、ボタン下にサブメニュー（トリミングボタン・保存ボタン・×ボタン）がポップアップ。
        - トリミングなしで保存した場合は、キャンバス比率の画像として保存。
        - トリミングは、デザイン領域の範囲内でトリミング可能。
        **トリミングは自由トリミングと、1:1、9:16などを選択して、トリミングすることが可能。**
        - 保存もしくは×ボタンを押すと、ロック解除。
    - 「表示倍率変更ボタン」
        - デザイン領域の表示倍率50%～300%で変更可能。
    - 「戻すボタン」
        - 1つ前の状態に戻す。
    - 「進むボタン」
        - 1つ先の状態に戻す。
    - 「AIへの指示ボタン」
        - 後述のAI用のメモをダイアログで開くことができ、UIで上書き編集可能。
    - 「ヒントボタン」（はてなマーク）
        - エディタの使い方説明ダイアログを開く。

- デザイン領域のベース
    - ページリロード時は、正方形で表示され、中に、本エディタの使い方が表示される。（この説明テキストは、uncontenteditable）
    - デザイン領域は背景白。
    - **ユーザー作成のデザイン領域内のテキストは、contenteditable。**
    - 画像ペースト禁止。
    - テキストボックス内には、純テキストとして、テキストペースト可能。

- 背景・その他
    - デザイン領域以外の背景は、ブラックを基調としたシンプルで洗練されたデザイン。
    - デザイン領域は縦横スクロール可能。


## 機能

### AIによるデザイン作成
- AntigravityのAIは、ユーザーの要求に基づいて、読み込んだデザインHTMLのコードを編集してデザイン生成する。
**デザインHTMLに関しては、tailwindは使わず、純粋なHTMLとCSSのみを使用する。**
- フォントは以下の15種類から選ぶ。
    - Noto Sans JP・Noto Serif JP・游ゴシック (Yu Gothic)・游明朝 (Yu Mincho)・メイリオ (Meiryo)・M PLUS 1p・Zen 角ゴシック New・Inter・Montserrat・Roboto・Playfair Display・Oswald・Poppins・JetBrains Mono・Times New Roman
- 各テキストボックス、各画像、各デザイン素材は必ず独立した要素として扱う。
- デザインエディタなので、ユーザーの意図をくみ取り、文字効果、グラデーション、図形の組み合わせ等を駆使し、洗練されたデザインを提供する。
- 画像に関しては、Antigravityが開いてるフォルダのルートに「images」フォルダを作成し、そこに画像データを置き、デザインHTMLでパスで参照する。
- また、AIは、ユーザー差し替え前提の仮の画像要素をデザインHTMLに置くことができる。
- また、AIは、ユーザー差し替え前提の仮の画像要素をデザインHTMLに置くことができる。
- 生成した各要素には一意のIDを付与する。
（エディタ上でユーザーが各要素をクリッ
クするとidが表示され、ユーザーとAIで共通認識できる。）

### エディタへの反映
- filesystemAPIにより、エディタは、読み込んだデザインHTMLのバッググラウンドでの上書き保存をリアルタイムで検知する。
- 検知した場合、ページ上のすべてのUIをロックし、読み込み中の表示を表示する。
**エディタ上でユーザーが名前を付けて保存、上書き保存をした場合はロックしないこと。**
- <!-- DESIGN_START -->と<!-- DESIGN_END -->の間のコードの変更のみを、エディタに反映する。（デザイン領域以外のスタイルの破壊を防ぐため）
- 変更のレンダリングが完了したら、ロックは維持したまま、メニューバーの下に、一時バーを表示する。
- 一時バーは、承認ボタン、破棄ボタン、比較ボタンのみとする（×ボタンはつけない）
**ページはロックされてるが、一時バーのみ操作可能にすること。**
- 承認した場合は、上書き保存後、ロック解除。
- 破棄した場合は、AIの変更を破棄し、元のデザインに戻して上書き保存。
- 比較ボタンを押すと、ロックしたまま、ウィンドウが左右に分かれ、変更前と変更後のスクショを比較できる。（この画面には×ボタンを付け、×ボタンを押すと、一時バーに戻れる）
**読み込み検知時、まず現在のキャンバスをメモリ上に一時画像（Base64等）として保存し、その後HTMLを更新して比較ビューを開く。**

### ユーザーによるテキストボックス微調整
- テキストボックスをクリックすると、上にメニューが表示される。
- メニューで、フォントカラー、フォントファミリー、フォントサイズ、太字を変更可能。
- また、テキストボックスないにキャレットが挿入され、テキストを直接編集可能。
**フォントファミリーは15種類程度用意する。**

### 拡大縮小、グループ化、要素移動
- また、UI上で、ホバーしている要素の拡大縮小が可能。
- テキストボックスおよび図形に関しては、縦横いずれかのみの伸長縮小も可能。
**テキストボックスの場合は、テキスト全体が表示され続けるようにレスポンシブする。**
- 要素をグループ化することができる。グループ化されたら、要素をまとめて移動、拡大縮小可能。
**HTMLタグに data-group-id="xxx" というカスタム属性を付与して保存することで、AIの読み取りを邪魔せずにグループ情報を永続化する。**
- 要素をクリックすると、ドラッグ移動可能となる。（グループおよび、子要素はまとめて移動）
**HTMLとして書き出す際は、余計なタグは消し、クリーンなタグ状態で出力する。**


### ユーザーによる画像差し替え
- デザイン上の画像要素をクリックすると、上にメニューが表示される。
- メニューには、枠線、枠線の色、角の丸み、トリミング、画像差し替えがある。
- 画像差し替えは、imagesフォルダ内の画像を選択して差し替えることができる。
**内部的には、パスで挿入される**
- 差し替え元と比率が違う場合は、縦横いずれかを合わせ、片方は余白を余らせる。object-fit: contain;
- トリミング後も、object-fit: contain;

### ハイクオリティ保証
- 出力HTMLにメタメッセージとして、一般的に一流のデザインを作るためのプロンプトを記述する。
- メッセージには、編集ルールも記載。（デザイン領域以外は絶対にいじらないこと。）    
- ユーザーの要求を言語化し、次回編集時に参照するための、AI用メモエリアをHTML中に用意。
    - ユーザーの要求
    - ユーザーからの注意
    - デザインコンセプト
    - メインカラー、サブカラー、アクセントカラー
    - 備考
    **すべて毎回更新すること**
    **絶対に日本語で書かせる。**
**このメタメッセージ群は、HTMLをブラウザで開いた際に、絶対に表示されないようにする。**
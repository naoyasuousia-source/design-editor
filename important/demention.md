<uneditable>

## 概要
これは、解決困難な要件を効率的に解決するためのファイルである。要件に関する重要事項を随時追記修正することで、AIが状況を動的に把握しやすくするとともに、他のモデルも即座に状況を把握できるようにする。

## 厳守ルール
- <uneditable>タグ内は絶対に編集しないこと。
- このファイル内の<## 見出し>は絶対に編集しないこと。
- rules.mdに従うこと。**rules.mdの例外事項をよく読むこと**
- ローカルサーバーは絶対に立ち上げないこと。
- ユーザーからの指示があるまで、セクション1,4は編集しないこと。

## セクション1記述方法

<requirement>
<content></content>
<current-situation></current-situation>
<remarks></remarks>
<permission-to-move>NG</permission-to-move>
</requirement>

</uneditable>

----------------------------------------
# 以下、AIが自動的に更新する部分
----------------------------------------

## 1. 未解決要件（移動許可がNGの要件は絶対に移動・編集しないこと）（勝手に移動許可をOKに書き換えないこと）

<requirement>
<content>
- A4サイズ選択時の仕様を改善する。
- 現在、A4新規作成時、A4の幅を超えた要素がデフォルトで配置されるため、デフォルト要素選択時に、デザイン領域の幅が拡大され、A4比率でなくなるというバグが発生する。
- デフォルトの要素を、A4領域に収まるものに変更してほしい。
- また、A4、9:16ともに、デフォルトデザインを、親子関係のないフラット要素のグループに変更してほしい。
</content>
<current-situation></current-situation>
<remarks>現仕様のロジックや過去のデバッグ履歴はimportant/を参照すること。</remarks>
<permission-to-move>OK</permission-to-move>
</requirement>

## 2. 未解決要件に関するコード変更履歴（目的、変更内容、変更日時）

- **デフォルトテンプレートの絶対座標化とバグ修正**
    - 目的: A4新規作成時に要素がキャンバス外にはみ出し、意図しないキャンバス拡大が発生するバグを解消するため。
    - 内容: `src/utils/templates.ts` 内の A4、9:16 用テンプレートにおいて、`left: 50%` や `transform: translateX(-50%)` などの相対指定を、計算済みの絶対座標（px）に変更。
    - 日時: 2026-01-04 21:55

## 3. 分析中に気づいた重要ポイント（試してだめだったこと、仮設、制約条件等...）

- **相対座標による誤算**: `expandCanvas` ロジック等で `left + offsetWidth` を計算する際、`left: 50%` と `transform: translateX(-50%)` が組み合わさっていると、要素の右端がキャンバス外にあると誤認されやすい。
- **フラット構造の徹底**: 新仕様では階層構造を排除しているため、テンプレートも最初からフラットで絶対座標を持つ構成にするのが最も安定する。

## 4. 解決済み要件とその解決方法

## 5. 要件に関連する全ファイルのファイル構成（それぞれの役割を1行で併記）

- `src/utils/templates.ts`: 新規作成時の初期HTMLテンプレート（A4, 9:16, SQUARE）を定義する場所。
- `src/types/editor.ts`: ページサイズのピクセル定義（PAGE_SIZES）を保持。
- `src/store/useEditorStore.ts`: キャンバスの動的拡大（expandCanvas）ロジックを保持。

## 6. 要件に関する機能の技術スタックと動作原理（依存関係含む）

- **絶対座標レイアウト**: CSS の `position: absolute` と `left`, `top`, `width` (px) を組み合わせて、ブラウザの拡大縮小やリサイズに強いレイアウトを構築。
- **Zustand**: キャンバスのサイズ拡張や状態管理に使用。
